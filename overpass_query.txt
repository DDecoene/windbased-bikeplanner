[out:json][timeout:300];
(
  // Find all cycle network relations within the bounding box for Flanders
  relation["type"="route"]["route"="bicycle"]["network"="rcn"](50.7, 2.5, 51.5, 5.9);
)->.routes;

// Get all ways and nodes that are part of these relations
(
  node(r.routes);
  way(r.routes);
);

out body;
>;
out skel qt;